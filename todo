# PhamaCount Web 優化清單

## 1. 拆分 ScanContent.tsx（798 行）— 最高優先級 ✅
- [x] `DrugCard` — 單個藥品卡片（編號圓圈、條碼比對、數量輸入、拍照）
- [x] `ErrorDrawer` — 異常清單側抽屜
- [x] `JumpDialog` — 跨頁跳轉對話框
- [x] `PhotoPreview` — 照片預覽 Modal
- [x] `BarcodeSearchBar` — 頂部條碼搜尋區
- [x] 自訂 hooks：`useBarcodeMatch`、`usePhotoCapture`、`usePagePersistence`

## 2. 共用型別定義 ✅
- [x] 抽取 `DrugItem`、`Manifest` 等重複定義的介面至 `src/types/`
- [x] `ScanContent.tsx`、`summary/page.tsx`、`manifests/page.tsx` 統一引用

## 3. UI/UX 改善 ✅
- [x] 返回按鈕圖示語意（`X` vs `ArrowLeft` 切換更直覺）
- [x] 條碼搜尋時非匹配項目淡出效果
- [x] 分頁導覽支援直接輸入頁碼跳頁
- [x] 清點完成後自動跳到下一頁或提示

## 4. Metadata & README ✅
- [x] `layout.tsx` metadata title 改為專案名稱
- [x] `README.md` 改為 PhamaCount 專案說明

## 5. E2E 測試更新 ✅
- [x] 檢查並修正過時的測試文案（如 "匹配成功!"、"清單 ID:"）
- [x] 確保測試與當前 UI 一致

## 6. 其他技術債 ✅
- [x] `import.ts` 中 `Buffer` 使用（Edge Runtime 不相容）
- [x] Migration 005 與 003/004 欄位重複問題
- [x] 補上 Supabase Auth 使用者認證