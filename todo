## 4. 關鍵業務邏輯與功能實作清單 (Development TODOs)

### 🟩 Phase 1: 圖片上傳、AI OCR 提取與自動分頁 (OCR & Import)
- [ ] **1.1 建立多圖上傳介面**
  - [ ] 支援拖曳或點擊批次上傳多張電腦清單截圖。
  - [ ] 暫存圖片並即時上傳至 Supabase Storage。
- [ ] **1.2 整合 Gemini API (Vision) 執行結構化 OCR**
  - [ ] 後端接收到圖片後，調用 Gemini 視覺模型。
  - [ ] 設計嚴謹的 Prompt，命其忽略原始表格樣式，精準提取出 `barcode` (條碼), `name` (藥名), `expected_quantity` (應有數量)。
- [ ] **1.3 多圖資料流合流與絕對排序**
  - [ ] 將多張截圖辨識出的藥品項目，依上傳與物理位置順序，統一編入全域順序池。
  - [ ] 自動賦予每筆藥物遞增的 `item_order` 欄位（1, 2, 3...）。
- [ ] **1.4 實作 44 項邏輯切片分頁**
  - [ ] 依據 `item_order` 每滿 **44 項目** 強制劃分為一頁，並寫入資料庫的 `page_number` 欄位。
- [ ] **1.5 實作「免預覽直接切換」流程**
  - [ ] 因截圖數據精準度高，後台辨識完成後，前端**不經由預覽確認頁**，直接自動跳轉並載入第 1 頁的分頁面板。
  - [ ] 將原始截圖以 `source_images: string[]` 的形式與該批清單綁定，供前端隨時調閱。

### 🟩 Phase 2: Web 端智能清點面板操作 (Operation Panel)
- [ ] **2.1 構建 RWD 分頁檢視 UI**
  - [ ] 預設顯示當前分頁（如第 1 頁，共 44 項），資料排列順序與實體紙本嚴格一致。
- [ ] **2.2 實作「字串包含 (String-In)」模糊篩選與全域自動跳轉**
  - [ ] 頂端常駐搜尋框，比對邏輯為：`barcode.includes(keyword) || name.includes(keyword)`。
  - [ ] 若搜尋結果存在於當前頁面，立即前端過濾高亮。
  - [ ] 若搜尋結果存在於其他分頁，系統**自動跳轉至目標分頁**並啟動篩選高亮。
- [ ] **2.3 實作數量即時比對與視覺反紅**
  - [ ] 篩選出的項目解鎖 `actual_quantity` (實際清點數量) 輸入框。
  - [ ] 比對邏輯：若 `actual_quantity != expected_quantity`，**該資料列立即觸發強烈視覺警告（整列反紅變色）**，狀態更新為 `error`；若相等則為 `completed`。
- [ ] **2.4 實作「不論對錯、強制拍照確認」機制**
  - [ ] 不論狀態為 `completed` 或 `error`（反紅），點擊確認時皆須啟用手機鏡頭（MediaCapture API）。
  - [ ] 拍攝實體藥物照片上傳至 Supabase Storage，關閉該列編輯狀態。
  - [ ] 提供防呆容錯，允許對已完成項目點擊縮圖重新拍照覆蓋。

### 🟩 Phase 3: 清單管理、異常覆核與結算 (Verification & Report)
- [ ] **3.1 實作清單列表與安全刪除**
  - [ ] 清單選擇列表中，每項右側新增「垃圾桶 🗑️」圖標。
  - [ ] 點擊後彈出客製化 Dialog 進行第二次確認，防範誤觸。
  - [ ] 確認後透過 PostgreSQL Cascade Delete 同步清空資料庫與 Storage 照片。
- [ ] **3.2 建立異常覆核獨立面板**
  - [ ] 最終提交前，一鍵集中篩選出整張清單中所有 `error`（反紅、數量不符）與 `pending`（未清點）項目，供藥師做最後實體對帳。
- [ ] **3.3 實作差異結案報告生成**
  - [ ] 當含有 `error` 項目的清單正式提交時，將批次標記為「有差異結案」。
  - [ ] 自動計算並在資料庫留存差額，鎖定該批清單不允許再編輯。